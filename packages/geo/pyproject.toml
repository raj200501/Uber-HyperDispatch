[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperdispatch-geo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["hyperdispatch-protocol"]

[tool.setuptools.packages.find]
where = ["src"]
