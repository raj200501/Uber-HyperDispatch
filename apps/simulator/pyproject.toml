[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hyperdispatch-simulator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27,<1", "hyperdispatch-protocol"]

[tool.setuptools.packages.find]
where = ["src"]
